const COMMIT='9d436c62f5cfbe78c84c9ef44fe8b5f8214d5cd1';
const CLUSTERS=['governance','runtime','cross-engine','evidence'];
export function assertFrameworkKnowledgeGraph(value){object(value,'knowledge');keys(value,['schemaVersion','id','frameworkCommit','route','title','summary','series','articles','evidenceChains','clusters'],'knowledge');if(value.schemaVersion!==1||value.id!=='framework-knowledge-graph'||value.frameworkCommit!==COMMIT||value.route!=='pages/framework/knowledge.html')throw new Error('knowledge identity drift');for(const key of ['title','summary'])text(value[key],key);for(const key of ['series','articles','evidenceChains'])list(value[key],key);if(!Array.isArray(value.clusters)||value.clusters.length!==CLUSTERS.length)throw new Error('knowledge clusters drift');value.clusters.forEach((entry,index)=>{object(entry,'cluster');keys(entry,['id','title','frameworkRoute','question'],'cluster');if(entry.id!==CLUSTERS[index]||entry.frameworkRoute!==`pages/framework/${entry.id==='cross-engine'?'cross-engine':entry.id}.html`)throw new Error('knowledge cluster route drift');text(entry.title,'cluster title');text(entry.question,'cluster question');});return true;}
export function resolveFrameworkKnowledgeGraph(value){assertFrameworkKnowledgeGraph(value);return{...value,series:[...value.series],articles:[...value.articles],evidenceChains:[...value.evidenceChains],clusters:value.clusters.map((entry)=>({...entry}))};}
function object(value,label){if(!value||typeof value!=='object'||Array.isArray(value)||Object.getPrototypeOf(value)!==Object.prototype)throw new Error(`${label} must be object`);}function keys(value,expected,label){if(JSON.stringify(Object.keys(value).sort())!==JSON.stringify([...expected].sort()))throw new Error(`${label} keys drift`);}function text(value,label){if(typeof value!=='string'||!value.trim()||/[\u0000-\u001f]/u.test(value))throw new Error(`${label} must be text`);}function list(value,label){if(!Array.isArray(value)||!value.length||new Set(value).size!==value.length)throw new Error(`${label} must be unique list`);value.forEach((entry)=>text(entry,label));}
